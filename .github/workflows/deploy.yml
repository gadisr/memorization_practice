# Deploy https://blindfoldcubing.com on push to main.
# Required repo secrets (Settings → Secrets and variables → Actions):
#   DEPLOY_HOST              droplet IP or hostname
#   DEPLOY_USER              SSH user
#   DEPLOY_SSH_PRIVATE_KEY   private key that can log in (no passphrase)
#   DEPLOY_PATH              app directory on the droplet (e.g. /root/memorization_practice)
# The server checkout must already exist, track origin/main, and have
# production.env + SSL in place.
#
# Production uses edge-proxy (container edge-nginx) on host 80/443.
# This workflow only rebuilds/restarts postgres, backend, frontend.
# It does NOT start nginx (would conflict with edge-proxy).

name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy on droplet
        uses: appleboy/ssh-action@v1.2.0
        env:
          DEPLOY_PATH: ${{ secrets.DEPLOY_PATH }}
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_PRIVATE_KEY }}
          envs: DEPLOY_PATH
          script_stop: true
          script: |
            set -eu
            cd "${DEPLOY_PATH:?set DEPLOY_PATH secret}"
            git fetch origin main
            git checkout main
            git pull --ff-only origin main
            docker compose -f docker-compose.yml up -d --build --no-deps frontend backend
            docker compose -f docker-compose.yml ps
